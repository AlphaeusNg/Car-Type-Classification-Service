# Send only files that Dockerfile can consume. Keep both model formats supported
# by MODEL_PATH; all local environments, datasets, Git data, and experiments
# remain outside the build context.
**
!Dockerfile
!requirements-api.txt
!api/
!api/*.py
!class_mapping.json
!model_manifest.json
!best_car_model.keras
